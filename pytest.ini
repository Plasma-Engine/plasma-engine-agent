[pytest]
addopts = -ra
testpaths = tests
pythonpath = app
markers =
    unit: Fast-running unit tests for the agent service.
    ai: Tests that exercise AI-specific configuration paths.
    performance: Micro-benchmarks that keep an eye on request latency.

